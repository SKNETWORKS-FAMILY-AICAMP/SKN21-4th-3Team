# ===========================================
# AI 상담 챗봇 - 환경변수 설정
# ===========================================
# 이 파일을 복사하여 .env 로 저장하고 값을 수정하세요.
# cp .env.example .env

# -------------------------------------------
# 실행 환경
# -------------------------------------------
# development: 개발 모드 (디버그 활성화)
# production: 프로덕션 모드 (Uvicorn ASGI)
FLASK_ENV=development

# -------------------------------------------
# 서버 설정
# -------------------------------------------
HOST=127.0.0.1
PORT=5000

# -------------------------------------------
# 보안 키
# -------------------------------------------
# 프로덕션에서는 반드시 변경하세요!
# 생성 방법: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# -------------------------------------------
# 데이터베이스 설정
# -------------------------------------------
# ▼ 로컬 개발 환경 (SQLite)
#   → DATABASE_URL을 비워두거나 주석 처리
# DATABASE_URL=

# ▼ AWS RDS 프로덕션 환경 (PostgreSQL)
#   → 아래 형식으로 RDS 엔드포인트 입력
# DATABASE_URL=postgresql://사용자명:비밀번호@RDS엔드포인트:5432/데이터베이스명
# 예시:
# DATABASE_URL=postgresql://admin:mypassword@mindcare-db.xxxxx.ap-northeast-2.rds.amazonaws.com:5432/mindcare

# -------------------------------------------
# OpenAI API
# -------------------------------------------
OPENAI_API_KEY=sk-your-api-key-here

# -------------------------------------------
# ChromaDB 설정
# -------------------------------------------
# 비워두면 기본값(data/vector_store) 사용
# CHROMA_DB_PATH=./data/vector_store

# 사용할 모델 (선택, 기본값: gpt-4o-mini)
OPENAI_CHAT_MODEL=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# -------------------------------------------------------------
# 앱 설정 (선택)
# -------------------------------------------------------------
# 환경: development / production
ENVIRONMENT=development

# 디버그 모드
APP_DEBUG=True

# 로그 레벨: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO
